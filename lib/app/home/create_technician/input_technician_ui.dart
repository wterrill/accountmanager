import 'package:flutter/material.dart';

import 'input_technician_backend.dart';

class InputTechnician extends StatelessWidget {
  const InputTechnician({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const UploadButton();
  }
}
